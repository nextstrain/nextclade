##### Column: Ns (missing)

Displays number of `N` characters (missing data) in the sequence.

Hovering mouse over a cell in this column will display a tooltip where intervals consisting of `N` characters are listed for the corresponding sequence.

Positions are 1-based.
