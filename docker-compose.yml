version: '3.7'

services:
  nextalign_dev:
    container_name: neherlab/nextalign_builder
    image: neherlab/nextalign_builder:latest
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        - USER=user
        - GROUP=user
        - UID=$UID
        - GID=$GID
    command: ['make', 'dev']
    volumes:
      - ./:/src
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    environment:
      - COLORTERM=truecolor
      - TERM=xterm-256color
    restart: 'no'
    init: true
    user: '${UID}'
    tty: true
